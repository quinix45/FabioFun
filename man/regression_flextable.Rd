\name{regression_flextable}
\alias{regression_flextable}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
APA style Regression Summary Table using `flextable`
}
\description{
Creates an APA style regression summary table that can be saved as a word document.
}
\usage{
regression_flextable(lm_object, 
                     title = "", 
                     var_names = NULL, 
                     intercept = TRUE)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
\item{lm_object}{A linear regression object generated by 'lm()'.} 
\item{title}{Table title (must be a `character`).} 
\item{var_names}{A `character` vector specifying the desired labels for every row. Must be of the same length as the number of table rows.} 
\item{intercept}{Should the intercept be included in the table?}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
Fabio Setti (fsetti@fordham.edu)
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{

## Test

lm_mod <- lm(Sepal.Length ~ Petal.Length + Petal.Width, iris)
 
reg_table <- regression_flextable(lm_mod)

# save to wiorking director as .docx document

flextable::save_as_docx(reg_table, path = "table.docx")
}
